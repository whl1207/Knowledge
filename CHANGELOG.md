# 更新日志

> AI-KM 的功能变更记录；软件内 **设置 → 帮助 → 更新日志** 与本文件同源。
> English version: [CHANGELOG.en.md](CHANGELOG.en.md)
> 约定：用户可见的改动追加到**最上方的日期小节**；只写基础 Markdown（`##` 日期、`###` 分类、`-` 条目、行内代码）。
> **精简要求（务必遵守）**：每条**一句话**，只写「做了什么 + 影响」；不写实现细节 / 代码路径 / 旧实现对比 / 举例；同类改动合并成一条；**一天条目尽量控制在 15 条以内（最多不超过 20 条）**。
> 排版提示：加粗标记要留空格（写成 `**导出 Word 工具** 与…`），不要紧贴中文标点，否则 `**` 不会渲染。

---

## 2026-09-24

### 新增

- **英文界面下的更新日志**：界面为英文时改为显示 `CHANGELOG.en.md`，不再一律显示中文内容。

### 优化

- **聊天标题跟随界面语言**：主页对话首次自动命名时按当前界面语言生成标题（英文界面出英文标题）。
- **更新日志移到仓库根目录并改名**：`docs/help/更新日志.md` → 根目录 `CHANGELOG.md`（英文版 `CHANGELOG.en.md`），README 与软件帮助页指向同一份内容。
- **README 重写**：改为仓库访客视角的结构（简介 / 功能 / 技术栈 / 安装构建 / 文档与更新日志 / 许可），并关联更新日志。
- **「Agent CLI」统一改名为「Agent脚手架」（英文 Agent Scaffold）**：导航栏、设置 → 功能开关、帮助中心整章、说明书与 README 一并改名（与命令行无关的 "CLI" 说法不再出现）；旧存档的当前面板与功能开关会自动迁移，老用户打开仍停在原来的面板。
- **仓库瘦身、内部材料下沉**：删掉根目录两个临时脚本、`docs/help` 下 6 个一次性构建/诊断脚本、`docs/plan` 13 篇设计文档（已合并为本地 `docs/设计决策记录.md`）；说明书与 Linux 打包指南移到 `manual/`，一键打包脚本移到 `scripts/build-linux-all.sh`；`docs/` 整体排除出仓库，README 链接同步更新。
- **使用说明书按软件现状同步**：修正小节编号错位（工作流 8.x / 技能 9.x / 待办 12.x / 设置 13.x）与交叉引用；导航总览补「数据画布 / 浏览器 Agent」、英文名改为 Agent Scaffold；工作流节点数与帮助页签数（13 个）等数字按代码现状核对更新。
- **大资源改为用户自备、仓库进一步瘦身**：`scripts/` 只保留 6 个构建 / 资源获取脚本（一次性排查与测试夹具不入库），离线地图包 `public/maps/*.mbtiles`（30MB）不入库并改由 `scripts/make-builtin-mbtiles.py` 自行构建；README 新增「Optional local assets」说明（地图包 / drawio 运行时 / Excalidraw 开发资源），说明书同步；实测提交体积 48MB → 17.6MB。
- **开发脚本整理**：一键打包 Linux 的 Windows 入口 `build-linux-all.cmd` 移入 `scripts/`（与 `.sh` 同目录，用法不变，双击即可），其余一次性排查脚本、测试夹具与探针产物（约 118MB）全部删除、本地不再保留。
- **`.gitignore` 补充**：忽略 Excalidraw 开发态资源、探针脚本的浏览器运行产物（上百 MB）、临时脚本 / 演示页，避免上传仓库时带上无用大文件。
- **授权改为非商业许可**：MIT → PolyForm Noncommercial 1.0.0，禁止任何商业使用（个人 / 学习 / 研究 / 教育 / 公益等非商业用途免费，允许非商业转发）；README 与安装包元数据同步。

### 修复

- **Agent脚手架英文界面下仍有中文**：状态栏（「共 / 出错」）、轮次名与跨轮引用（第1轮 → Round 1，打开旧任务时自动归一，指令里的引用一并迁移）、结果页与导出 Excel 的列名（`_行号` / `_轮次·` / `_来源`）与默认结果列名、日志页线程标签、图谱悬停信息与类型名、实例标签名（Agent脚手架 1 → Agent Scaffold 1）都改为跟随界面语言。
- **导出列改名后结果页与 Excel 取不到该列的值**：自定义列名后表头用的是原名、数据用的是新名（结果页显示空白、导出的 Excel 列错位），现已统一为同一个列名。
- **Linux 一键打包不出包**：修复 `scripts/build-linux-all.cmd` 打包失败的问题，现在可一次产出 x64（deb + AppImage）与 arm64（deb）三个安装包。

---

## 2026-09-23

### 修复

- **Agent脚手架：只删了几行却满屏「已变更」**：以前用「整行文本」比对，Excel 重新保存后单元格写法一变（数字千分位、多余 `.0`、前后空白）就全被当成改过；现在改用**归一化指纹**判变更（只把真的改了内容算变更），并对不上指纹时靠**首个非空单元格**认回同一行——删几行就只报删了几行，结果也不会错位；旧任务文件没存归一化指纹时不逐行判变更、只报增删（重跑一轮后恢复）。
- **Agent脚手架：「同步源表」的对位与提示**：以前按整行文本比对，Excel 存一次换了写法就会被当成改过、对位失败时还会整表退回按行号对位并弹「只认出 712/21021 行」这类自相矛盾的提示；现在按**归一化指纹**认行（Excel 换写法也认得出）、**不再自行改变对位方式**，这类说明也不再弹窗，改为在左下角状态栏精简显示（补一句「只算行数增减」）。
- **Agent脚手架：「同步源表」后结果也跟着同步**：同步 / 重新导入后立刻重写当前轮次快照，切轮次 / 对比 / 保存用的都是同步后的结果；源表里被删掉的行，其**多余结果会自动删掉**（结果跟着源表走，各模式一致）；只有对位不可靠（删掉的超过一半）时才只标「已移除」不删，可用结果页的「已移除 N」查看后手动处理。
- **Agent脚手架：增量统计不再被误配放大**：增删位置无法可靠确定时（任务文件较旧、行被搬动、表内容变动很大），增量改为**只报行数增减（净差）**，不再出现「新增 872 / 移除 889」这种成对冒出的大数字；识别可靠时仍精确到具体行。
- **Agent脚手架：源表增删行后结果会错位**：以前按行号硬对位，在源表中间插一行就会让后面所有行的结果挂到别人身上；现在按行指纹对位，增删 / 重排行都不会再串位。
- **Agent脚手架：导出的 Excel 列序与结果页预览不一致**：自定义拖动「导出列」顺序后，Excel 表头不再按内部默认次序，而是与列表顺序完全一致。
- **DeepSeek 只保留一个来源**：设置里的「DeepSeek Responses」已合并进「DeepSeek」（用「接口样式」切换 Chat Completions / Responses API），主页、预设、工作流、知识库与 OCR 的来源下拉不再出现重复的 DeepSeek Responses；旧存档自动迁移（来源列表、禁用标记与连接状态一并归一）。
- **主页对话界面统一**：聊天列表开关移到聊天标题左侧（**☰** 纯图标、不用按钮盒子样式，**默认不显示聊天列表**，点一下展开）；空白聊天时不再显示标题输入框、输入区居中收窄并给出引导提示，发送第一条消息时平滑下滑回贴底；**有消息时同样包在一张圆角矩形输入卡片里（通栏，不限最大宽度）**；卡片内文字输入区与模式选择 / 附件 / 语音 / 发送等控件均改为**无边框**；**“停止生成”按钮占用发送按钮的位置**（生成 / 执行中显示停止，空闲显示发送）；**“正在生成 / 正在检索 / 连接测试”等状态提示改为只显示一个图标**（文字放在图标的悬停提示里、尺寸与附件按钮一致），不再占输入框上方的空间；聊天列表侧栏的**搜索 / 新建按钮与每条聊天记录统一为 30px 行高**、与右侧标题框对齐；**集群运行时该对话在聊天列表里也会显示运行中的转圈图标**（与智能体运行时一致）；**生成 / 回复过程中不再禁止上传附件**（上传的附件随下一条消息发出）；消息与输入卡片之间留出间距；**输入卡片与卡片内的下拉框 / 输入框统一用正常背景色**（不用菜单 / 卡片底色），下拉悬浮与选项列表也不再变成菜单高亮色；输入框宽度不再超出卡片；**已关联项胶囊移到模式下拉左侧**；智能体写入本地网页（`.html`）时，工具卡片在「展开详情」旁多了**「打开」按钮**，用内置浏览器直接看效果；**系统 / 模型服务返回的错误**（如“模型服务返回错误 / Context size has been exceeded”）除对话里的一条消息外，还会在输入行显示一个**红色感叹号图标**（悬停看完整错误、点一下关闭）。

### 新增

- **Agent脚手架：新增「同步源表」按钮**：任务开着时在 Excel 里增删 / 改了行（或文件夹里加了 / 删了文件），点一下「详情」页工具栏的「同步源表」即可重读源表并重新对位（不用重开任务）；同一个表格文件再次「导入」也改为**替换行**（不再追加导致行数翻倍）。
- **Agent脚手架：能看出源表的「增量」**：源表增删 / 改过行后重读表格（或点「同步源表」），变化的行会标上 `新增` / `已变更`（**已变更 = 还是同一行、结果仍留在该行上**，只是源数据改过、建议重跑），被删掉的行标为「已移除」（结果保留在结果页里）；工具栏给出「+新增 ~变更 -移除」摘要，筛选面板新增「增量」可只看新增行或已变更行。
- **Agent脚手架：增删行后能直接看出变化在哪**：工具栏的「+新增 ~变更 -移除」摘要里，点「**新增** / **变更**」就在下方表格里筛选（再点取消），悬停可看列级差异（新增 / 丢失 / 顺序变化）与对位说明（不再需要单独的「查看变化」面板）。
- **Agent脚手架：结果页能追到任务行**：结果表新增「任务行」列，点行号直接跳到「详情」页并高亮该行；源表里已被删掉的行，其结果仍留在结果页并标「已移除」，结果页会出现「**已移除 N**」按钮（有才显示）；详情页工具栏的「移除 N」同样可点，直接跳到结果页看它们。
- **Agent脚手架：结果数多于任务行数时能看清原因**：结果页底部会给出统计（如「8 条结果 / 6 个任务行：一行多条 1 条、无对应任务行 2 条、已移除 1 条」）；找不到对应任务行的「异常」结果可用工具栏的「**异常 N**」按钮一键筛出，筛出后点「**删除这些**」直接删掉（旁边的「**清除**」只取消筛选、不删结果）；工具栏新增「**同步结果**」：按当前任务行重建结果（每个任务行只保留它自己产出的记录、行号与数据重新对齐），数量与数据都和任务行一致。
- **Agent脚手架：导出列可自定义列名**：导出列列表里点列名即可改名（回车 / 失焦生效，留空恢复默认），结果页表头与导出的 Excel 都用这个名字；改过名的列会标出原名（结果页表头悬停也能看到）。
- **Agent脚手架：结果页可逐条删除结果**：结果表最右侧新增删除图标，删掉该条记录（结构化提取会同步移除它写回原表的数据），任务行本身保留、可重跑。
- **Agent脚手架：重跑结果可选择「替换还是保留」**：「输出 → 重跑结果」新增选项（默认「替换该行原有结果」），重跑前先清掉这行上次产出的记录，结果不会越跑越多（跑失败时旧结果保留）；要多行汇总成一条时改选「保留并合并」。

### 优化

- **Agent脚手架：实例栏的「开始 / 暂停」按各实例的任务状态显示**：该实例有待处理的行才出现「开始」，正在运行就只显示「暂停」，没活可干时两个都不占位（切换实例标签时跟着变）。
- **Agent脚手架：导出大 Excel 不再卡住**：导出改为分片写入（每 2000 行一片，片间让出界面）并在状态栏显示「导出 x/y 行」进度，导出中按钮变灰显示「导出中…」；内容与列序 / 自定义列名均不变。
- **Agent脚手架：并发上限放宽到 500**（「运行 → 并发数」），大批量任务行可一次跑完；日志页最多同时显示 60 个线程标签，避免标签挤满。
- **Agent脚手架：输出面板的说明改为悬停提示**：「输出方式」「输出字段」「导出列」下方的常驻说明移到标题 / 下拉上，界面更紧凑。
- **Agent脚手架：输出面板底部留白与左右对齐**：导出列列表不再贴到面板底边（右下留白一致）。

## 2026-09-22

### 修复

- **修复本地模型网关丢工具参数导致智能体反复报错**：部分网关（GPUStack / LM Studio 等）最终回的调用参数为空或被截断，工具会拿空参数执行并报「code 参数必填」，模型还会反复重试；现在改用参数流式原文兜底，确实无法解析时给出明确提示（含是否被截断），不再静默当成空参数。
- **修复工具卡片显示**：已流式显示出来的代码/命令不再被随后的空参数覆盖成 `{}`；`run_code` 失败时显示真实错误，不再一直显示「程序执行中…」。
- 修复帮助页数据画布与浏览器步骤编号的类型检查错误，确保项目可以正常通过渲染层类型检查。
- **修复行详情「重跑本行」会连带其它待处理行一起跑**：未运行时点重跑会从最早的待处理行开始整批执行；现在只跑这一行（日志会说明还有多少待处理行未纳入，需整批继续时回任务页点启动）。
- **修复「文件夹来源」的扩展名 / 排除目录看似没保存**：这两个设置一直都在任务文件里，但读取后不会回填到输入框（显示成默认值，再点一次扫描还会把它覆盖）——现在打开任务时会回填，编辑后（失焦 / 回车）也即时写回配置。
- **修复「扫描文件夹」生成任务行报错**：选择文件夹来源后点扫描会提示「扫描失败: An object could not be cloned.」——已修复，现在可正常按文件生成任务行（已存在的文件仍会自动跳过）。
- **Agent脚手架：导入 / 读取大表格时进度回到状态栏最左侧**：读取行数每 5000 行刷新一次（与之前同名功能一致），不再只是界面卡住、看不到进行到哪了。
- **Agent脚手架：对比页行多时终于能上下滚动了**（之前内容被裁掉、滚不动）；单元格预览弹窗不再被默认宽度限制成 400px，对比表最外层也不再有两层边框。

### 新增

- **搜索设置支持自定义（内网）搜索源**：左侧 **+** 可添加任意数量，类型可选「SearXNG 兼容实例」或「HTTP JSON 接口」，支持填 IP:端口、`{query}` 占位、请求头 / Token 与结果字段映射；与内置源一同拖动排序、启用与测试（左上角 ↺ 重置会连同自定义源一起清除）。
- **主页智能体运行中也能直接输入要求（引导）**：智能体 / 集群执行时输入框不再锁死，Enter 即把输入作为「引导」投递给正在执行的智能体（不打断本轮，下一个 step 带上）；引导直接插在步骤时间线里它生效的位置（后续步骤排在它下面），比另发一条气泡更直观；若刚好本轮已结束，则按新一轮任务正常发送。
- **Agent脚手架：结果页新增「清除结果」**：只清结果（每行结果 / 状态 / Token / 计时 / 过程快照 + 提取数据表），任务行与配置保留，行回到「待处理」可重跑。
- **draw.io 图表：支持「导出为 → PDF」与「文件 → 打印」**：改走应用本地打印通道（离线可用，只输出当前页，打印会弹系统打印对话框）。
- **Agent脚手架：新增「耗时筛选」**：与「步数筛选」同规格（`< ≤ > ≥ =` + 数值；默认秒，可写 `90` / `1.5m` / `2m30s` / `1h`），未运行的行按 0 计——筛出推理过快的行后，同样用「重跑 → 当前筛选结果」重跑。
- **Agent脚手架：筛选收进一个面板，并支持批量标记状态**：状态 / 搜索 / 结果 / 行号 / 步数 / 耗时 统一在「筛选」面板里配置，面板内实时显示命中行数；命中行可一键标记为待处理 / 跳过 / 已完成 / 失败（「待处理」= 待执行，下次启动即跑），或直接「重跑命中行」（标记待处理并立即执行）。
- **Agent脚手架：筛选面板增加「结果为空」开关**：点一下即可筛出结果为空的行（等价于结果筛选里的 `|''` 写法，可与关键字叠加），方便筛选后重跑。
- **Agent脚手架：详情页工具栏精简**：去掉「重跑 ▾ / 导入表格 / 重置」——重跑对象改为当前筛选命中行（面板内一键，运行中会插队立即执行，未运行则标记待处理并启动）；导入与重置在「任务」页。
- **Agent CLI：实例栏的开始 / 暂停 / 保存改为只作用于当前标签的任务**（原来是对全部已打开实例）；需要批量操作时按住 Ctrl / ⌘ / Alt 点击这三个按钮。
- **Agent脚手架：纯 LLM 推理也统计行 Token**：以前只有智能体模式的行有 Token，现在「纯 LLM 推理」（逐列写回 / 结构化提取）的行同样显示输入 / 输出 Token（详情表、行详情与导出列都生效），重跑按本轮口径覆盖。
- **Agent脚手架：行操作集中到行详情面板**：详情表格的「操作」列只显示状态文字（去掉状态下拉与重跑 / 删除图标）；底部按钮栏顺序调整为 状态选择 → 停止（仅执行中）→ 重跑（其他情况）→ 删除，右侧关闭。
- **Agent脚手架：行详情可单独「停止」这一行**：执行中的行 → 取消它的会话 / 请求，排队中的行 → 移出队列，随后标记为「跳过」——其它行与整批继续跑，不影响别的任务。
- **Agent脚手架：对比页点单元格即可渲染预览**：点任意一轮的产出会弹窗用 Markdown 渲染展示（标题显示所属轮次），同时对比表始终列出全部行（含只在其它轮出现的行，该轮显示 `—`）。

### 优化

- **Agent脚手架：详情筛选可按「结果或指定字段」筛**：筛选面板的「结果」一行升级为「字段 + 关键字」——默认仍是结果列，也可选具体的提取字段 / 原表列（含逐列写回的新列）；关键字仍支持 `|` 分隔多关键字，「为空」开关跟着所选列走。
- **Agent脚手架：结果页改为渐进式加载并支持搜索 / 筛选 / 跳转**：不再固定只预览前 500 行，滚动到底自动继续加载（每批 200 行）；工具栏新增全列关键字搜索、按列筛选（值包含；只选列不填值 = 只看该列非空的行）与行号跳转高亮；导出仍然写全部行（不受搜索 / 筛选影响）。
- **Agent脚手架：大文件分段推理不再产成多行**：结构化提取时同一任务行被切成多段推理，现在默认把各段结果按字段合并为**一条记录**（文本字段按段顺序拼接去重）——一个文件最终只产出 1 条数据；需要在文件里提取多条独立记录时，可在「输出 → 分段结果」改为「每段各成一条」。
- **Agent脚手架：文件夹来源的任务行改为打开任务时自动生成**：选中「文件夹（扫描本地文件）」并选好目录的任务，下次打开实例（或双击 .task）会自动扫描该目录重建任务行，并把已保存的结果 / 状态按行合并回来（与表格来源自动重读源表一致）。
- **Agent脚手架内置帮助精简**：去掉「DeepSeek Responses」一段（该说明在任务页的能力提示里已有），帮助页更短。
- **统一命名：脚手架改叫「Agent脚手架」**：去掉「通用流水线 / 统一流水线」这套说法，帮助中心、说明书、实例标题、设置与提示文案中英全部改名（英文 Agent Scaffold）；存量实例的自动标题会自动迁移，手动改过名的不动。
- **Agent脚手架：任务页不再有「两栏」中间态**：窗口窄于约 900px 就直接变单栏（配置 → 输入 → 输出），宽屏仍是三栏。
- **Agent脚手架：任务页「导出列」列表占满输出面板的剩余高度**：窗口越高列表越长（以前固定 176px），列表内部仍可滚动。
- **Agent脚手架：对比页去掉了「只看问题行」按钮**：不一致行本来就有橙色底与 ⚠ / ? 标记，不再需要一个筛选开关（以前点它还会把页面筛空）。
- **Agent脚手架：对比页「轮次」芯片改为至少保留一轮**：关掉一个轮次就只看剩下的那轮（不再整表空掉），也无法把轮次全部关掉。
- **Agent脚手架：行详情「输出」运行中改为流式渲染正文**：不再显示 Markdown 源码，标题 / 列表 / 表格边跑边成形（渲染按 ~150ms 合并，避免逐字重渲染卡顿）；跑完仍是同款渲染。
- **Agent CLI：空实例状态更干净**：没有实例时隐藏实例导航栏（那 5 个按钮此时没作用）；引导页也精简为一句说明 + 「新建任务 / 打开任务」两个按钮（去掉快捷新建与底部提示）。
- **Agent脚手架：行详情底部改为「状态」下拉**：取代原来的「重跑 / 跳过」两个按钮，可直接把该行改成待处理 / 已完成 / 失败 / 跳过（只改状态、结果不动；「待处理」= 待执行，回任务页点启动就跑它），执行中的行状态不可改。
- **Agent脚手架：图谱改为「布局收敛后一次性平滑居中」**：不再布局过程中反复调整视图（原来会抖动）；打开时先给一个按节点数估算的较广视角，布局跑完后自动把所有节点框进视口并居中（自己缩放 / 拖动后不再自动调整）。
- **Agent脚手架：行详情底部一行收拾齐整**：页签右上角的「复制 / 编辑」移到下方按钮栏（与状态、删除、关闭同一行），底部按钮与下拉高度统一，状态下拉去掉底色与左侧文字标签（说明改走悬停提示）。
- **Agent脚手架：导出列改为竖向表单**：原表列 / 运行信息列 / 各轮结果合到一个列表里，逐项开关勾选、拖动手柄调整顺序（顺序即导出表的列顺序），并标注每项来源。
- **Agent脚手架：行详情「过程」的工具调用改为默认单行摘要**：与主页对话的工具盒子同款（图标 + 工具名 + 状态 + 折叠箭头），点一下才展开参数与结果（超长自动滚动），不再一进来就铺满半屏。
- **批量智能体：系统提示词与主页 / 工作流 / 集群完全一致**：去掉批量专有的结尾「语言要求」段落（输出语言改由任务与预设提示词自身决定），只保留「剔除询问用户工具」这一处差异。
- **知识库文件视图**：搜索框从底部状态栏移到顶部工具栏右侧，底部状态栏不再显示当前路径，文件数改到右下角显示。
- **draw.io 图表：改为手动保存**：编辑不再自动写盘（保存按钮出现橙色小点表示未保存），点 💾 或按 `Ctrl+S` 才写回文件并提示「已保存」；切标签 / 换主题 / 关窗口时仍会静默兜底保存，不会丢内容。
- **Agent CLI：状态反馈统一放在底部状态栏**：运行状态（转圈 + 「执行中…」等状态文字）移到状态栏最左侧，左列「启动」按钮运行时只变灰、不再显示「运行中…」；实例栏「保存」与结果页「导出 Excel」的「保存中 / 已保存 / 已取消 / 失败」也在状态栏左侧显示，成功与取消不再弹提示框，几秒后自动消失（失败详情悬停可见），一次保存多个实例时汇总成一行。
- **智能体读取大文件不再只读开头一段**：超出单次长度上限时会给出截断位置，可自动续读直到读完（PDF / Word / Excel 单次 2 万字符，纯文本约 100KB）。
- **工具新增技能寻址（`skill` 参数）**：`read_file` / `list_dir` / `search_files` 可传技能名，路径相对该技能目录解析——技能里 `regulations/x.md` 这类相对写法可直接用，不再依赖工作目录。

### 修复

- **智能体工具的工作目录不再被技能改动**：预设勾选技能时不再把工具工作目录切到技能库（以前问「工作区有哪些文件」会答成技能文件夹），现在始终是当前工作区。
- **智能体（聊天 / 批量 / 工作流 / 集群）：技能库统一在同一处加载**：技能文件夹改动后立即生效（不再沿用旧目录的技能），批量执行时也能正常注入技能库清单与预设勾选的技能指令。
- **`skill` 工具不再报「技能不存在」**：技能名的大小写 / 下划线差异可自动匹配；名字确实不存在时会返回可用技能清单，便于模型改正后重试。
- **Agent脚手架：行详情「输出」里的 Markdown 不再被整段撑高**：原来每行源码换行都被当成硬换行，同一段报告的高度虚增约七成；现在只对流式输出 / 错误等等宽文本生效，标题与段落间距也收紧，同一份内容约降到原来的四成、页面更紧凑。
- **Agent脚手架：结构化提取没解析出 JSON 的行不再丢内容**：以前模型答的是散文（没按 JSON 输出）时，这一行既没有提取数据、原文也被丢掉，导出的数据表里根本看不到该行——现在原文保留在该行（行详情「输出」可见），导出会**自动带上「原始输出」列**把该行一并写出；跑完还会提示未解析的行数，若「空内容处理」选了自动重跑则会同样重试。
- **Agent脚手架：重跑后行 Token 不再累加**：行的 Token（与开始 / 结束时间、步数同口径）只表示本轮用量——以前重跑会从上一次的数值上继续加，越跑越大。
- **帮助页文案修正**：Agent脚手架说明里的星号不再原样显示；英文版一处重复片段已清理。
- **Agent脚手架：导出 Excel 更稳**：单元格超过 Excel 上限（32767 字符）或含非法控制字符时不再整份导出失败——超长内容自动截断并标注；导出失败会在状态栏提示（原来只写在日志里）。

---

## 2026-09-21

### 新增

- **Agent脚手架：新增「轮次」（多轮处理）**：任务页可新建 / 切换 / 重命名 / 删除轮次（每轮各存配置快照与结果，任务文件分槽保存，旧文件自动成为第 1 轮）；可用 {{第1轮.结果}} 引用前轮产出，⑂ 一键把本轮结果展开成新一轮任务行，「对比」页签并排看各轮产出并标出不一致行。
- **Agent脚手架：新增「输出方式」「预设」「导出列」**：输出可选「单独结果 / 结构化提取」，预设一键套用四种旧脚手架形态；导出 Excel 可点选原表列与运行信息列合成一张表（未选择时行为不变）。
- **Agent脚手架：新增「随任务文件保留」与图谱开关**：图谱数据 / 运行日志可选随任务文件保存（默认不保留，文件更小）；也可在任务配置里关闭图谱（不做轨迹投影、隐藏图谱页签）。
- **Agent脚手架：新增「文本（每行一条）」来源**：粘贴文本按行生成任务行，每行可「直接使用 / 读取本地文件 / 抓取网页」（抓取即链接采集，含链接跟随与页数上限）。
- **Agent脚手架：详情页新增「步数筛选」**：可按推理步数（<、≤、>、≥、=）筛选行，未运行的行按 0 步计——配合「重跑 → 当前筛选结果」即可一键补跑步数不够的行。
- **Agent脚手架：推理步数随任务文件保存**：每行步数不再只在内存里（原来只有勾选「图谱数据」才间接留存），重开后步数列、步数筛选与导出照常可用；过程快照仍按需保留。
- **Agent脚手架：页面拆为任务 / 详情 / 结果 / 对比 / 图谱 / 日志 / 帮助页签**：详情新增运行元信息与结构化提取字段列，结果页即导出预览；另新增「子任务标题」与来源路径显示。
- **轨迹图谱：新增「访问路径」视图 + 智能体 / MCP 服务节点**：每行一个「智能体」节点（未调用工具的也显示为单个节点），边指向访问过的网页 / 文件 / 搜索 / 知识库与调用过的 MCP 服务；节点上限可调（默认 2000）。
- **批量智能体 / 主页 PTC**：「行号范围」筛选与「重跑 ▾」选区批量重跑（执行前预览命中行数）；主页新增 PTC 对话模式（模型写一段程序经 `run_code` 组合调工具）。

### 优化

- **Agent CLI：唯一脚手架收敛定名为「Agent脚手架」 + 实例操作改进**：原四种脚手架并入为它的四种预设（旧 `.task` 打开自动转换），实例操作按钮常驻导航栏、「数据画布」独立为顶层模块；新建实例不再预建任务文件（首次保存时选位置），保存反馈移到状态栏左侧。
- **Agent脚手架：任务页布局收敛 + 输出字段合并**：三列布局、操作栏吸顶、各面板独立滚动、窄窗口自动降列；「任务模板 / 目标 / 源列」统一为「任务指令 + 数据选择」，「输出字段」合并为一张列表（写回与结构化共用），写回结果可在行详情查看 / 编辑。
- **Agent脚手架：行详情改为「任务 / 过程 / 输出」三页签**：页签在左、运行计数（开始 / 结束 / 耗时 / 步数 / Token）在右，复制 / 编辑按钮紧跟页签；页签选择会保持，连续打开其他行不用反复切换。
- **轨迹图谱：默认视图按任务自动选择**（有网页行 → 爬取轨迹，否则 → 访问路径）+ 空态说明；新增节点从视口外侧移入，工具栏改为彩色圆点图例（单击即筛选）。
- **批量智能体 / PTC**：未运行时重跑某段不再带跑其它待处理行、运行中批量重跑一次性入队、任务模板默认 10 行；PTC 子调用入参不再回灌模型上下文。
- **设置 → 基础：删除「Agent CLI」开关块**（只剩Agent脚手架一个，无需再关）。

### 修复

- **Agent脚手架：任务语言跟随**：预设未写系统提示词时，英文任务会被内置中文提示词带成中文作答——现统一追加「用任务语言输出」。
- **轨迹图谱：数据一变化就整张图重排（持续闪烁）**：改为增量更新（节点 / 边复用、仅结构变化才小幅加热）；窗口最小化改用微任务刷新；补齐小图箭头颜色并修掉首帧缩放把图缩没。
- **智能体预设：改「来源」后模型下拉为空**；来源探测失败不再中断 AI 配置加载。
- **Qwen3-ASR 升级**：新增「流式（分段准实时）」（停顿即切段，文字边说边出）与网页服务接口 / 参数（语种、标点断句、说话人、单行上限），原 OpenAI 兼容端点仍可用。
- **语音识别「按住说话」不出结果**：Whisper API / Whisper Python / Qwen3-ASR 三个引擎的录音数据被提前清空，结果始终为空——已修复。

---

## 2026-09-20

### 新增

- **Agent CLI 支持多实例**：每个实例 = 一个任务 + 一个 `.task` 文件（上限按类型）；新增实例导航栏与 ☰ 菜单（从文件打开 / 新建实例 / 开始全部 / 暂停全部 / **保存全部**），标签显示运行状态、悬停看文件与预计剩余时间、可重命名或关闭，运行中切走仍继续跑，进入实例时才自动读取任务文件。
- **任务文件（`.task`）统一带类型标记**：双击即打开（或激活）对应实例，同一文件不允许被两个实例关联；**数据画布**也改用 `.task` 保存（变更后 1.5 秒防抖自动保存）。
- **新增内置 MCP 服务「PostgreSQL 查询」**（7 个工具）：只读 SQL 与作者消歧，三层只读保护，自动适配 schema，支持对外 HTTP 暴露。
- **作者消歧增强**：支持批量（一次最多 200 条）、容忍句点与缺中间名 / 中间首字母（如 `Mark Perazella` → `Mark A. Perazella`）、主档案合并碎片档案，输出可精简。
- **Ollama / LM Studio 模型面板**：列出类型（对话 / 视觉 / 嵌入）、是否加载、生效或配置的上下文上限，可一键加载 / 卸载；嵌入模型下拉只列嵌入模型。
- **实例标签悬停显示进度与预计剩余时间**（每秒刷新）。

### 优化

- **模型设置页**：刷新入口统一到「API 状态」，LM Studio 列表网格对齐且不再限高，Ollama / LM Studio 不再重复显示「上下文窗口」，不可达时只给一条可读原因。
- **MCP 调用更少出错**：工具清单附必填 / 可选参数签名，`mcp_call` 缺必填参数提前拦截，Office 工具漏传会话 ID 时自动复用唯一已打开文档，参数别名统一归一。
- **PostgreSQL 查询**：自动适配 schema、报错给出可操作指引、并发调用加服务端闸门与结果缓存。
- **作者消歧大幅提速**：新增姓名索引快路（单条 15–20 秒 → 毫秒级）、去掉触发全表扫的写法、批量共用一次扫描（批量 3 条约 0.8 秒）。
- **托管区与 ☰ 菜单**：托盘悬停按实例逐条显示进度与剩余时间；「暂停全部」给出提示并让出主线程，不再像卡死。
- **Agent CLI 细节**：标题栏与其它面板一致、实例导航栏高度与图标对齐、底部状态栏不再被顶出可视区。

### 修复

- **批量表格**：跳转后向上滚动无效 / 上方空白；拖动划选文字不再误触发行详情或误关弹窗。
- **打开大任务偶发「表格解析缓存不存在」**：实例层合并并发加载，主进程解析缓存改为引用计数。
- **控制台看不到新建的智能体**（事件桥丢弃未知会话 → 改为建占位视图并定期轻量拉取）。
- **表格定向推理：导入表列一多「源列」列表被挤到右侧看不见**（改纵向单列、表头吸顶）。
- **PostgreSQL**：语句超时被错误钳到 1 秒（未配置时按默认 30 秒 / 50 行）；`pg_resolve_author` 在超大库不再超时。
- **Excel / Word MCP 工具**：漏传会话 ID 时提示更准确，参数别名（`workbook_id` / `file_path` 等）统一归一。

---

## 2026-09-19

### 新增

- **内置文献检索 MCP 服务**（免 Key）：CrossRef / OpenAlex / arXiv / PubMed / Europe PMC 五源搜索 + 作者检索 + 撤稿核查 + 开放获取全文（9 个工具）。
- **联网搜索源可配置多源**：免 Key 的 Bing / 百度 / DuckDuckGo、自建 SearXNG、带 Key 的博查 / 智谱 / Tavily / Brave；支持依次回退或并行聚合、逐源测试、拖动排序与一键重置；工作流「网络搜索」节点同源。
- **MCP 服务可配置调用超时**（留空 = 60 秒）。
- **关闭按钮可折叠到托管区**（新增「关闭按钮行为」设置）：折叠后关窗不退出、后台任务继续跑；托盘悬停显示任务数与进度，右键菜单可显示主窗口 / 设置 / 后台任务 / 退出。

### 优化

- 「搜索」设置页布局与「模型」页一致；Bing 中文查询改写可开关；搜索失败提示带来源与通道；文档链接改用自带浏览器打开。

### 修复

- **`mcp_call` 偶发 30 秒超时**：默认超时放宽到 60 秒并可按服务配置，文献检索多源工具改并发（最坏约 50 秒 → 约 15 秒）。
- 帮助页「内置 MCP 服务」卡片的加粗 / 行内代码标记不渲染。
- 「搜索」设置页文案过长导致横向滚动条。

---

## 2026-09-18

### 新增

- **文档智能操作**：浏览视图状态栏 ✨ 与当前文档对话，可附带「整篇 / 当前章节 / 选中文字」（上限约 12 万字符，超出会提示已截断）。
- **导出 Markdown / Word / PDF**：状态栏 ⬇ 菜单；导出 Word 弹出另存为对话框，可选公文 / 中文论文 / 英文论文（APA 7）/ 自定义 `.docx` 模板并指定保存位置。
- **上下文窗口与占用圆环**：主页聊天标题右侧显示上下文占用率；「模型」页每个来源底部显示只读的窗口分母与覆盖值。
- **批量智能体运行**（Agent CLI）：表格驱动批量任务，支持占位符模板、并发 / 重试 / 续跑与结果导出 Excel。
- **表格定向推理**（Agent CLI）：按「源列 → 目标列」配置指令，由 LLM 逐行推理填值。
- **凭据管理表格化**：名称 / 脱敏密钥 / 更新时间 / 操作，回车即可连续新增，同名覆盖。
- **常用操作新增**：导出 Markdown、备份数据、打开数据目录、清空会话日志。
- **设置导航调整**：新增「关联」；「系统」取消，路径设置与常用操作并入「基础」，系统状态并入「其他」。
- **帮助页新增「更新日志」页签**（即本文件），移除「常见问题」页签。

### 优化

- 帮助页：设置说明长度统一，新增 drawio 图表、文档智能操作、上下文窗口等章节。
- 工具管理为逐工具粒度（18 个），预设与集群成员共用同一份逐工具列表。
- 更新日志向前追溯至 2026-07，并精简条目文字。

### 修复

- 帮助页设置说明中的 HTML 标签不渲染；内置 MCP 服务第三项未显示。

---

## 2026-09-17

### 新增

- **Office 文档读写内置 MCP 服务**（Word + Excel，29 个函数）：查找 / 整段重写 / 插入表格与 Markdown / 样式设置，区域读取 / 筛选排序 / 分组聚合 / 批量写入；写回只改对应 XML 部件并自动 `.bak` 备份。
- **导出 Word 工具** `export_word` 与 **工作流 Word 导出节点**（默认关闭，可在工具管理中启用）。
- **智能体模式支持上传图片**（多模态）与文本附件。
- **模型来源管理**：来源可逐个停用、支持多个自定义来源并按对话绑定；GPUStack 裸地址自动补 `/v1-openai`。
- **DeepSeek**：接口样式切换（Chat Completions / Responses）、账户余额查询、联网搜索。
- **OCR 多来源可选**（Ollama / LM Studio / DeepSeek / 自定义等），并修复远端 Ollama 复读导致的整页识别失败。
- **设置项**：推理强度、来源连接测试行、文档目录共享面板。
- **关闭保护**：后台任务运行时关窗先提醒。

### 优化

- 会话日志开始记录 system 提示词；集群角色分组与预设入口整理。

### 修复

- 深色主题下复选框 / 单选按钮不可见；Excel 与 Word 的 OOXML 写回兼容性。

---

## 2026-09-15 ~ 09-16

### 新增

- **draw.io 图表视图**：`.drawio` / `.dio` 文件在浏览 / 源码编辑 / 可视编辑 / 思维导图 / 演示视图中直接打开内置编辑器，**完全离线**；创建文件弹窗新增 draw.io 类型。
- **图表智能操作（AI）**：右下角 ✨ 用自然语言新增 / 调整（位置·尺寸·文字·颜色·层级·连线）/ 删除图形，画布预览后确认写入，可取消回退；支持「自动可调 / 仅新增」。
- **图表协同编辑**：走 draw.io 官方 diffSync 增量同步，仅主机写盘与发起 AI 操作。
- **drawio 内置 MCP 服务**（44 个函数）：智能体直接读写 `.drawio` XML（增删改、布局、Mermaid / CSV / SVG 互转等）。
- **内置服务对外 HTTP 暴露**：供 Claude Desktop、Cursor 等外部 MCP 客户端接入。

### 优化

- 导出 Word 的数学公式优先转成 Word 原生**可编辑公式**（OMML）；块编辑器支持公式段落。

### 修复

- draw.io「导出为 → 下载 / 另存为到设备」点了没反应（改为系统原生保存对话框）；保存后其它窗口不刷新；子窗口关不掉；点设置闪加载遮罩。

---

## 2026-09-12 ~ 09-14

### 新增

- **设置改为独立窗口**；新增**文件关联（默认打开方式）**与**窗口缩放**设置。
- 阅读 / 编辑类组件统一的**底部状态栏**（目录、搜索、导出、朗读、字号、阅读进度）。
- **精确编辑工具** `replace_in_file` / `multi_replace`，工具结果带**差异预览**。
- **思维导图**保存后保留视图、讲解 / 演示模式；**PDF 预览**；**文档面板与 PPT**；**离线地图包（MBTiles）**。
- **集群并入主页对话模式**：移除独立模块与 `.swarm` 预案文件，成员即 Agent 预设。
- **MCP 运行时迁入主进程**，渲染层通过 IPC 委托。

### 优化

- 源码目录分层与 `@` 别名；文件独立窗口内容同步；数学公式（MathJax）渲染修复。

### 修复

- 设置窗口污染主窗口导航；Leaflet 地图交互异常。

---

## 2026-09-07 ~ 09-11

### 新增

- **局域网共享**：手机 / 其他电脑用浏览器访问聊天界面，并可共享知识库目录。
- **浏览器 Agent**：AI 标签自动回收；收藏夹导入导出（Netscape HTML 兼容）。
- **技能商店**、**会话日志分段加载**、**MCP 设置左右布局**、**图片浏览同目录导航**。

### 优化

- Agent CLI 目录结构与脚手架标签内嵌；知识库问答单页支持多模型来源；文件树与首屏启动性能优化；全局右键菜单样式统一。

### 修复

- 思维导图保存后视图被重置；Excalidraw 协作者光标导致崩溃；知识库文件视图「已修改」徽标不触发。

---

## 2026-09-04 ~ 09-06

### 新增

- **逐工具开关**（Agent 预设 / 集群成员）；Agent 工具参数流式展示、`<think>` 深度思考区、提问弹窗 Markdown 渲染。
- **本地 ONNX TTS**（Kokoro / Piper）与 **TTS / ASR 设置拆分**；FunASR 本地与流式标点。
- **Python-lite 沙箱**、**PTC（Code Mode）**、**多工作区支持**、**工作流新节点**。
- **RAG 策略增强**：Agentic 证据重排、Hybrid 混合策略、社区 / 本体召回加速、批量测试预热面板。
- **待办**显示 / 隐藏与周视图；**数据画布**表单录入。

### 修复

- Mermaid 渲染统一（查看弹窗、PPT、高度限制）；文件采集结构化 JSON 输出。

---

## 2026-08-26 ~ 08-31

### 新增

- **局域网协同文件编辑**（Yjs CRDT：代码编辑器 + Excalidraw 白板，成员光标与编辑权限）。
- **远程文件共享**：共享文件夹以只读「远程工作区」形式接入文件树。
- **LLM 多自定义来源**；**Word / PDF 导出带图**（MHTML）；考试标签页与题库分页。

### 优化

- 启动首屏与文件树性能优化；子窗口系统菜单标题；知识库自动准备与配置项扩展。

---

## 2026-08-18 ~ 08-24

### 新增

- **本地 ASR**（ONNX Whisper）与转写标点校验；**媒体播放器**；**技能并入设置**（兼容 Codex 技能格式）。
- **集群**：自动轮数、事件时间线、管理器 Agent、集群切换运行隔离、能力节点图谱。
- **工作流**新增节点（M1 / M2）、测试结果展示。

### 优化

- 迷你 Python 沙箱；多工作区根目录；文件树展开状态持久化；数据画布主页双模式。

---

## 2026-08-12 ~ 08-17

### 新增

- **AI 服务主进程化**（模型请求统一走主进程）；**MCP 主进程化**；集群 MCP 控制。
- **Agentic RAG**；Python 沙箱支持 SciPy；技能管理并入设置；技能商店雏形。
- 地图 GeoJSON 图表与看板模式；工具调用卡片默认折叠并实时显示参数。

### 修复

- 首页聊天标题命名、步骤指示器空白、集群预案切换隔离等一批问题。

---

## 2026-08-04 ~ 08-08

### 新增

- **可视编辑**（块编辑）与公式支持；编辑视图的 **AI 修改差异**；工作流测试结果显示；信息采集多表合并。

### 优化

- dev / prod 双环境共存；集群成员选择与运行状态展示。

---

## 2026-08-01

### 新增

- 多智能体**集群类型**（通用 / 控制）、态势地图与装备、方案（规划）管理。

> 说明：上述「集群控制 / 态势地图 / 装备与方案」能力后续已拆分为独立软件 **SwarmPlanner**。

---

## 2026-07-20 ~ 07-21

### 新增

- 离线地图（MBTiles）原型；方案（规划）管理系统原型。

> 说明：这两项后续随集群规划能力一并迁出为独立软件 **SwarmPlanner**。
